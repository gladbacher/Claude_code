// Metric definitions for each entity type
// percentile scores in data are pre-inverted for "lower is better" metrics
// so the radar always shows "more area = better performance"

const METRICS = {
  broker: [
    {
      key: 'hitRate',
      label: 'Hit Rate',
      description: 'Quotes converted to bound policies',
      unit: '%',
      higherIsBetter: true,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'quoteVolume',
      label: 'Quote Volume',
      description: 'Normalized submission volume vs peer group',
      unit: 'score',
      higherIsBetter: true,
      format: v => v.toLocaleString() + ' quotes',
    },
    {
      key: 'gwpRetention',
      label: 'GWP Retention',
      description: 'Renewal GWP as % of expiring GWP',
      unit: '%',
      higherIsBetter: true,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'policyRetention',
      label: 'Policy Retention',
      description: 'Policies renewing as % of expiring count',
      unit: '%',
      higherIsBetter: true,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'avgPremium',
      label: 'Avg Premium',
      description: 'Average written premium per policy',
      unit: '£',
      higherIsBetter: true,
      format: v => '£' + v.toLocaleString(),
    },
    {
      key: 'lossRatio',
      label: 'Loss Ratio',
      description: 'Claims incurred / GWP — lower is better',
      unit: '%',
      higherIsBetter: false,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'panelRank',
      label: 'Panel Rank',
      description: "Position score on insurer's broker panel",
      unit: 'score',
      higherIsBetter: true,
      format: v => '#' + v + ' on panel',
    },
    {
      key: 'bindRate',
      label: 'Bind Rate',
      description: '% of referred risks ultimately bound',
      unit: '%',
      higherIsBetter: true,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'ntuRate',
      label: 'NTU Score',
      description: 'Not Taken Up rate — lower NTU = higher score',
      unit: '%',
      higherIsBetter: false,
      format: v => v.toFixed(1) + '% NTU',
    },
    {
      key: 'claimsFreq',
      label: 'Claims Frequency',
      description: 'Claims per 100 policies — lower is better',
      unit: 'per 100',
      higherIsBetter: false,
      format: v => v.toFixed(1) + ' per 100 policies',
    },
  ],

  segment: [
    {
      key: 'lossRatio',
      label: 'Loss Ratio',
      description: 'Claims incurred / GWP — lower is better',
      unit: '%',
      higherIsBetter: false,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'gwpGrowth',
      label: 'GWP Growth',
      description: 'Year-on-year gross written premium growth',
      unit: '%',
      higherIsBetter: true,
      format: v => (v > 0 ? '+' : '') + v.toFixed(1) + '%',
    },
    {
      key: 'policyGrowth',
      label: 'Policy Growth',
      description: 'Year-on-year policy count growth',
      unit: '%',
      higherIsBetter: true,
      format: v => (v > 0 ? '+' : '') + v.toFixed(1) + '%',
    },
    {
      key: 'hitRate',
      label: 'Hit Rate',
      description: 'Quotes converted to bound policies',
      unit: '%',
      higherIsBetter: true,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'policyRetention',
      label: 'Policy Retention',
      description: 'Policies renewing as % of expiring count',
      unit: '%',
      higherIsBetter: true,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'avgPremium',
      label: 'Avg Premium',
      description: 'Average written premium per policy',
      unit: '£',
      higherIsBetter: true,
      format: v => '£' + v.toLocaleString(),
    },
    {
      key: 'claimsFreq',
      label: 'Claims Frequency',
      description: 'Claims per 100 policies — lower is better',
      unit: 'per 100',
      higherIsBetter: false,
      format: v => v.toFixed(1) + ' per 100',
    },
    {
      key: 'profitability',
      label: 'Profitability Index',
      description: 'Underwriting profit as % of GWP after expenses',
      unit: '%',
      higherIsBetter: true,
      format: v => (v > 0 ? '+' : '') + v.toFixed(1) + '%',
    },
  ],

  product: [
    {
      key: 'lossRatio',
      label: 'Loss Ratio',
      description: 'Claims incurred / GWP — lower is better',
      unit: '%',
      higherIsBetter: false,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'combinedRatio',
      label: 'Combined Ratio',
      description: 'Loss ratio + expense ratio — lower is better',
      unit: '%',
      higherIsBetter: false,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'gwpGrowth',
      label: 'GWP Growth',
      description: 'Year-on-year gross written premium growth',
      unit: '%',
      higherIsBetter: true,
      format: v => (v > 0 ? '+' : '') + v.toFixed(1) + '%',
    },
    {
      key: 'policyRetention',
      label: 'Policy Retention',
      description: 'Policies renewing as % of expiring count',
      unit: '%',
      higherIsBetter: true,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'claimsFreq',
      label: 'Claims Frequency',
      description: 'Claims per 100 policies — lower is better',
      unit: 'per 100',
      higherIsBetter: false,
      format: v => v.toFixed(1) + ' per 100',
    },
    {
      key: 'avgClaimSize',
      label: 'Claim Severity',
      description: 'Average claim settlement — lower is better',
      unit: '£',
      higherIsBetter: false,
      format: v => '£' + v.toLocaleString(),
    },
    {
      key: 'expenseRatio',
      label: 'Expense Ratio',
      description: 'Operating expenses as % of GWP — lower is better',
      unit: '%',
      higherIsBetter: false,
      format: v => v.toFixed(1) + '%',
    },
    {
      key: 'premiumAdequacy',
      label: 'Premium Adequacy',
      description: 'Premium vs technical price — 100 = fully adequate',
      unit: 'index',
      higherIsBetter: true,
      format: v => v.toFixed(0) + ' index',
    },
  ],
};

// Color palette for entities
const COLORS = {
  primary: {
    broker: '#00c4ff',
    segment: '#ff7b35',
    product: '#4ade80',
  },
  comparison: '#f59e0b',
  bg: {
    page: '#0d1b2a',
    card: '#162032',
    chartArea: '#0f1d2e',
  },
};
